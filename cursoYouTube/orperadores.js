/* Operadores desconocidos en javaScrip */

let a = 0;

a = 5;

let b = 2;
 
let c;

// a es la base y b es el exponenete. 
c = a ** b; // este operador indica que se debe de realizar una potencia 
console.log(c);

// Son operadores simplificados, el primer simbolo indica la operaciónn a 
//realizar y el segundo yo intuyo que asigna el valor a la variable a. 
a += 5;
a -= 5;
a *= 2;
a /= 2;
a %=2; // Nos muestra el resto de una división, comunmente se utiliza para saber si un numero es par o no. 
a ** 5; // Es la simplificación del operador de exponenciaión.
console.log(a);